# Current version dependencies
Flask==3.0.0
Werkzeug==3.0.1

# Dependencies for improved version:
Flask-Caching==2.1.0
Flask-Cors==4.0.0
pandas>=2.1.0
# Frontend libraries are supplied via CDN in the template; do not install as Python packages
